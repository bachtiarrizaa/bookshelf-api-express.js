const createBook = require('./createBook');
const getAllBooks = require('./getAllBook');
const getBookById = require('./getBookById');
const updatedBook = require('./updatedBook');
const deleteBook= require('./deleteBook');

module.exports = {
  createBook,
  getAllBooks,
  getBookById,
  updatedBook,
  deleteBook,
};
